[project]
name = "backend"
version = "0.1.0"
description = "API Flask para gestión de inventario de equipos de cómputo"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0.0",
    "flask-cors>=4.0.0",
]
